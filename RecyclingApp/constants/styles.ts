import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    checkListHeader: {
        fontSize: 20,
        padding: 15,
    },
    checkListText: {
        fontSize: 16,
    }
});

export default styles;