import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    headerText: {
        fontSize: 20,
        padding: 15,
        textAlign: 'center',
    },
    defaultText: {
        fontSize: 16,
    },
    textBox: {
        paddingBottom: 20,
    },
});

export default styles;